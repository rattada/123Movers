@model IEnumerable<_123Movers.Models.TreeViewModel>

@foreach (var treeNode in Model)
{
    <ul> 
        @if (treeNode != null)
        { 
            <li><a href="#">
                @treeNode.NodeName
            </a>
               @* @if (treeNode.ChildNode.Count > 0)
                {
                    @Html.Partial("ChildNode", treeNode.ChildNode)
                }*@
            </li> 
        }
    </ul>
}