@if (Request.IsAuthenticated) {
    <text>
       <div class="col-md-9"> <p class="pull-right" style="margin-top:8px;">Hello, @User.Identity.Name!</p> </div> <div class="col-md-3"> @*@Html.ActionLink(User.Identity.Name, "Manage", "Account", routeValues: null, htmlAttributes: new { @class = "username", title = "Manage" })!*@
        @using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm" }))
        {
            @Html.AntiForgeryToken()
           <button onclick="javascript:document.getElementById('logoutForm').submit()" class="btn btn-primary">Log off</button>
        }</div>
    </text>
} else {
   @* @Html.ActionLink("Log in", "Login", "Account", routeValues: null, htmlAttributes: new { id = "loginLink" ,@class = "btn btn-primary"})*@
    
}
